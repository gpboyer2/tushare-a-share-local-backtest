/**
 * @fileoverview
 * 共享契约层的共享契约导出聚合模块，负责定义前后端共享的类型约定和数据结构。
 *
 * 主要职责：
 * - 对外暴露：模块级常量、类型或默认导出。
 * - 为当前前端页面、组件或共享契约层提供明确的单一职责能力。
 *
 * 实现方式：
 * - 把页面、服务端和共享包共用的接口结构抽到同一处维护。
 * - 降低前后端对字段和返回格式理解不一致的风险。
 */

export * from "./workbench";
export * from "./database";
